# Runtime dependencies for exposed application features are declared in requirements.txt
# so clean Windows installations do not fail after users select those features.
# TorchCodec is intentionally not required by this app. If it is installed,
# keep it compatible with your torch version or remove it.
# Demucs is a core packaged dependency because the Audio page exposes
# background-music preservation. It is declared in requirements.txt.
