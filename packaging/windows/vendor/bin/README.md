Place trusted Windows x64 `ffmpeg.exe` and `ffprobe.exe` in this directory.

Do not commit binaries without recording their source URL, version, license,
and SHA-256 hashes in `SHA256SUMS.txt`. The build script refuses to create a
release when either executable is missing.
